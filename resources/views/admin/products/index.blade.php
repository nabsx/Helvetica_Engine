<x-admin-layout title="Produk">
<div class="max-w-7xl">
    <header class="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p class="text-xs font-bold uppercase tracking-[0.18em] text-emerald-600">Catalog control</p><h1 class="mt-2 text-3xl font-black tracking-tight text-slate-950">Produk & harga</h1><p class="mt-2 max-w-xl text-sm leading-6 text-slate-500">Kelola harga jual, HPP, pajak, dan ketersediaan produk dari satu tempat.</p></div><div class="flex gap-2"><a href="{{ route('admin.categories.index') }}" class="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-bold text-slate-700 shadow-sm">Kategori</a><a href="{{ route('admin.products.create') }}" class="rounded-xl bg-slate-950 px-4 py-3 text-sm font-bold text-white shadow-sm hover:bg-slate-800">+ Produk baru</a></div></header>
    @if(session('success'))<div class="mt-6 rounded-xl border border-emerald-100 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{{ session('success') }}</div>@endif
    @if(session('error'))<div class="mt-6 rounded-xl border border-red-100 bg-red-50 px-4 py-3 text-sm font-semibold text-red-700">{{ session('error') }}</div>@endif
    <div class="mt-8 grid gap-3 sm:grid-cols-3"><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs font-bold uppercase tracking-wide text-slate-400">Total produk</p><p class="mt-2 text-2xl font-mono tabular-nums font-semibold">{{ $products->total() }}</p><p class="mt-1 text-xs text-slate-500">Dalam katalog</p></div><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-xs font-bold uppercase tracking-wide text-slate-400">Status harga</p><p class="mt-2 text-2xl font-mono tabular-nums font-semibold text-emerald-600">Nett</p><p class="mt-1 text-xs text-slate-500">Pajak dihitung saat checkout</p></div><div class="rounded-2xl border border-amber-100 bg-amber-50 p-5"><p class="text-xs font-bold uppercase tracking-wide text-amber-700">Kontrol margin</p><p class="mt-2 text-2xl font-mono tabular-nums font-semibold text-amber-900">HPP aktif</p><p class="mt-1 text-xs text-amber-700">Snapshot tersimpan per transaksi</p></div></div>
    <form class="mt-6 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-[1fr_240px_auto]"><input name="search" value="{{ request('search') }}" placeholder="Cari nama produk..." class="rounded-xl border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100"><select name="category_id" class="rounded-xl border-slate-200 bg-slate-50 px-4 py-3 text-sm"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category_id') == $category->id)>{{ $category->name }}</option>@endforeach</select><button class="rounded-xl bg-slate-800 px-5 py-3 text-sm font-bold text-white">Terapkan filter</button></form>
    <div class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="hidden grid-cols-[2fr_1fr_1fr_1fr_auto] gap-4 border-b border-slate-100 bg-slate-50 px-5 py-3 text-[11px] font-bold uppercase tracking-wide text-slate-400 md:grid"><span>Produk</span><span>Harga nett</span><span>HPP</span><span>Pajak</span><span>Aksi</span></div>@forelse($products as $product)<div class="grid gap-4 border-b border-slate-100 px-5 py-5 last:border-0 md:grid-cols-[2fr_1fr_1fr_1fr_auto] md:items-center"><div class="flex items-center gap-3">@if($product->image)<img src="{{ $product->image }}" alt="{{ $product->name }}" class="h-12 w-12 rounded-xl object-cover">@else<div class="flex h-12 w-12 items-center justify-center rounded-xl bg-slate-100 text-xs text-slate-400">IMG</div>@endif<div><p class="text-xs text-slate-400">{{ $product->category?->name ?? 'Tanpa kategori' }}</p><p class="font-bold">{{ $product->name }}</p><p class="mt-1 text-xs {{ $product->stock <= $product->low_stock_threshold ? 'font-bold text-amber-600' : 'text-slate-500' }}">Stok {{ $product->stock }} unit</p></div></div><div><span class="text-xs text-slate-400 md:hidden">Harga nett</span><p class="font-mono tabular-nums font-semibold text-emerald-700">Rp{{ number_format($product->price, 0, ',', '.') }}</p></div><div><span class="text-xs text-slate-400 md:hidden">HPP</span><p class="font-mono tabular-nums font-semibold text-slate-700">Rp{{ number_format($product->cost_price ?? 0, 0, ',', '.') }}</p></div><div><span class="inline-flex rounded-full bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-700">{{ $product->tax_name ?? 'Tanpa pajak' }} {{ $product->tax_rate ? number_format($product->tax_rate, 2) : '0' }}%</span></div><div class="flex gap-2"><a href="{{ route('admin.products.edit', $product) }}" class="rounded-lg bg-slate-100 px-3 py-2 text-sm font-bold text-slate-700">Edit</a><form method="POST" action="{{ route('admin.products.destroy', $product) }}" onsubmit="return confirm('Hapus produk ini?')">@csrf @method('DELETE')<button class="rounded-lg bg-red-50 px-3 py-2 text-sm font-bold text-red-600">Hapus</button></form></div></div>@empty<div class="p-10 text-center text-sm text-slate-500">Belum ada produk.</div>@endforelse</div><div class="mt-6">{{ $products->links() }}</div>
</div></x-admin-layout>
