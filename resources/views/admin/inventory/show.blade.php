<x-admin-layout title="Riwayat Inventory">
<div class="space-y-8">
    <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><a href="{{ route('admin.inventory.index') }}" class="text-sm font-bold text-emerald-700">← Kembali ke inventory</a><p class="mt-5 text-xs font-bold uppercase tracking-[0.2em] text-slate-400">Audit trail produk</p><h1 class="mt-2 text-4xl font-black tracking-tight text-slate-950">{{ $product->name }}</h1><p class="mt-2 text-sm text-slate-500">Riwayat perubahan stok yang append-only dan dapat direkonsiliasi.</p></div><div class="flex gap-2"><a href="{{ route('admin.inventory.export', $product) }}" class="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-bold text-slate-700 shadow-sm">Export CSV</a><a href="{{ route('admin.inventory.adjust', $product) }}" class="rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white shadow-sm hover:bg-emerald-700">Sesuaikan stok</a></div></div>
    @if(session('success'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{{ session('success') }}</div>@endif
    <section class="grid gap-4 sm:grid-cols-3"><div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p class="text-xs font-bold uppercase tracking-wider text-slate-400">Stok sekarang</p><p class="mt-3 font-mono text-4xl font-black tabular-nums text-slate-950">{{ number_format($product->stock) }}</p><p class="mt-1 text-sm text-slate-500">unit on hand</p></div><div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p class="text-xs font-bold uppercase tracking-wider text-slate-400">Threshold rendah</p><p class="mt-3 font-mono text-4xl font-black tabular-nums text-amber-700">{{ number_format($product->low_stock_threshold) }}</p><p class="mt-1 text-sm text-slate-500">unit minimum</p></div><div class="rounded-2xl border border-slate-200 bg-slate-950 p-6 text-white shadow-sm"><p class="text-xs font-bold uppercase tracking-wider text-slate-400">Ledger entries</p><p class="mt-3 font-mono text-4xl font-black tabular-nums">{{ number_format($history->count()) }}</p><p class="mt-1 text-sm text-slate-400">catatan audit</p></div></section>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-100 px-6 py-5"><h2 class="font-black text-slate-950">Movement history</h2><p class="mt-1 text-sm text-slate-500">Setiap baris menunjukkan stok sebelum dan sesudah event.</p></div><div class="divide-y divide-slate-100">@forelse($history as $movement)<div class="flex flex-col gap-4 px-6 py-5 sm:flex-row sm:items-center sm:justify-between"><div class="flex items-center gap-4"><div class="flex h-10 w-10 items-center justify-center rounded-xl {{ $movement->quantity_delta >= 0 ? 'bg-emerald-100 text-emerald-700' : 'bg-rose-100 text-rose-700' }} font-black">{{ $movement->quantity_delta >= 0 ? '+' : '−' }}</div><div><div class="flex items-center gap-2"><span class="rounded-full px-2.5 py-1 text-[11px] font-bold uppercase {{ $movement->type === 'sale' ? 'bg-rose-50 text-rose-700' : ($movement->type === 'refund' ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600') }}">{{ $movement->type }}</span><span class="text-xs text-slate-400">{{ $movement->created_at->format('d M Y, H:i') }}</span></div><p class="mt-1 text-sm font-semibold text-slate-800">{{ $movement->note ?? ucfirst($movement->type).' inventory movement' }}</p><p class="mt-1 text-xs text-slate-400">oleh {{ $movement->user->name }}</p></div></div><div class="flex items-center gap-8 sm:text-right"><div><p class="text-xs text-slate-400">Perubahan</p><p class="font-mono text-lg font-black {{ $movement->quantity_delta >= 0 ? 'text-emerald-700' : 'text-rose-700' }}">{{ $movement->quantity_delta > 0 ? '+' : '' }}{{ $movement->quantity_delta }}</p></div><div><p class="text-xs text-slate-400">Stok</p><p class="font-mono text-sm font-bold text-slate-700">{{ $movement->stock_before }} → {{ $movement->stock_after }}</p></div></div></div>@empty<div class="px-6 py-16 text-center text-sm text-slate-500">Belum ada riwayat inventory.</div>@endforelse</div></div>
</div>
</x-admin-layout>
