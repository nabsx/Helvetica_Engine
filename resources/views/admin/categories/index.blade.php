<x-admin-layout title="Kategori"><main class="mx-auto max-w-5xl"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p class="text-sm font-semibold uppercase tracking-widest text-emerald-600">Admin panel</p><h1 class="mt-1 text-3xl font-black">Kategori menu</h1></div><a href="{{ route('admin.products.index') }}" class="rounded-xl bg-slate-900 px-4 py-3 text-sm font-bold text-white">Kembali ke produk</a></div>@if(session('success'))<div class="mt-6 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif @if(session('error'))<div class="mt-6 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{{ session('error') }}</div>@endif<div class="mt-8 grid gap-6 lg:grid-cols-[320px_1fr]"><form method="POST" action="{{ route('admin.categories.store') }}" class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">@csrf<h2 class="font-bold">Tambah kategori</h2><input name="name" required placeholder="Contoh: Kopi" class="mt-4 w-full rounded-xl border-slate-300 px-4 py-3"><button class="mt-3 w-full rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white">Tambah</button></form><section class="space-y-3">@foreach($categories as $category)<div class="flex items-center gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><form method="POST" action="{{ route('admin.categories.update', $category) }}" class="flex flex-1 gap-2">@csrf @method('PUT')<input name="name" value="{{ $category->name }}" required class="min-w-0 flex-1 rounded-xl border-slate-300 px-3 py-2 text-sm"><button class="rounded-lg bg-slate-100 px-3 py-2 text-sm font-semibold">Simpan</button></form><span class="text-xs text-slate-400">{{ $category->products_count }} produk</span><form method="POST" action="{{ route('admin.categories.destroy', $category) }}" onsubmit="return confirm('Hapus kategori ini?')">@csrf @method('DELETE')<button class="rounded-lg bg-red-50 px-3 py-2 text-sm font-semibold text-red-600">Hapus</button></form></div>@endforeach</section></div></main></x-admin-layout>
